<div class="container">
  <h1>All Movies</h1>
  <div class="movie-list-container" >
    <%= @movies.each do |movie| %>
    <div class="title">
    <h4> <strong>Title:</strong> <%= movie.title %><br></h4>
      <%= link_to movie_path(movie) do %>
      <div class="overview">
        <img src="<%= movie.poster_url %>" class="img-fluid" style="max-height: 200px;">
      <% end %>
        <div class="description"><p><strong>Overview:</strong> <%= movie.overview %><br></p>
        <p><strong>Rating:</strong> <%= movie.rating %></p>
        <%= link_to "Delete", movie_path(movie), class: "btn btn-danger" , data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
